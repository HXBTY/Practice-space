// 时间分片渲染大数据文件
import { defineComponent, ref } from "vue";

export default defineComponent({
    setup() {
        return () => (
            <>
                <ul class="ul-box"></ul>
            </>
        )
    }
})