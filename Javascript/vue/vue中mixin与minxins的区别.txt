mixin用于全局混入，会影响到每个组件的实例，通常插件都是这样初始化的。
mixins则是用于拓展组件（局部使用，只有引入了mixin的组件才能使用），如果多个组件中有相同的业务逻辑，就可以将这些逻辑剥离出来，通过mixins混入代码，比如上拉，下拉加载数据

1. 如果当组件内的数据与mixin的数据相冲突时，已组件内部为主
2. 如果mixin与组件内部都有相同的钩子函数时，则这两个钩子函数会合并成一个数组，并且mixin的钩子函数先调用，后调用组件内部钩子
3. 如果混入对象和组件内部的methods、components、directives冲突时,以组件内部的为准